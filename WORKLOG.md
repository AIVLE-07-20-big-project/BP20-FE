# BP20 Frontend 작업 로그

> 코드·설정 변경 시 이 파일의 작업 로그, 결정 사항, 미해결 이슈를 함께 갱신한다. 비밀번호·토큰 등 민감정보는 기록하지 않는다.

## 2026-07-22

### 작업 로그

- 프로젝트 루트에 작업 기록을 지속 관리하기 위한 `WORKLOG.md`를 생성했다.
- Figma 기반 초기 화면의 서비스명과 프로젝트명을 `BP20`으로 변경했다.
- Figma/Figma Make 생성 흔적과 불필요한 파일·의존성을 정리했다.
- React·TypeScript 프로젝트를 `app`, `pages`, `features`, `entities`, `shared`, `mocks` 중심으로 재배치했다.
- 인증, 점주, 관리자, IAM, 오류 화면을 역할과 기능에 맞게 페이지 디렉터리로 분리했다.
- Tailwind 소스 탐색 경로를 수정해 디렉터리 이동 후 깨진 스타일을 복구했다.
- 타입 검사, 프로덕션 빌드와 주요 화면 렌더링을 확인했다.

### 결정 사항

- 전역 초기화와 라우팅은 `app`, 화면은 `pages`, 사용자 기능은 `features`, 도메인 모델은 `entities`, 공통 요소는 `shared`에서 관리한다.
- UI 문구는 한국어를 기본으로 사용하고 BP20 브랜드명을 일관되게 사용한다.
- 점주와 관리자 화면은 역할별 페이지로 분리하되 공통 컴포넌트는 재사용한다.

### 미해결 이슈

- 반응형 화면, 접근성, 브라우저 호환성과 주요 사용자 흐름 테스트를 보강해야 한다.
- 온라인 주문, 쿠폰, 세트상품 화면은 백엔드 도메인 확정 후 구현해야 한다.

## 2026-07-24

### 작업 로그

- 공통 API 클라이언트와 개발 서버 프록시를 추가해 Spring Boot API 호출 기반을 구성했다.
- 실제 로그인, JWT 저장, 새로고침 시 사용자 인증 복원, 역할별 라우트 보호와 만료 세션 정리를 구현했다.
- 기존 프로토타입 계정 전환과 목업 화면은 실제 계정 흐름과 분리해 그대로 유지했다.
- 점주 온라인 커머스 화면을 매장 등록, 온라인 판매 개시·종료, 상품 등록·온라인 판매 등록, 할인 관리 API와 연결했다.
- 고객·쿠폰 화면을 고객 등록·조회, 쿠폰 발급·조회·회수 API와 연결했다.
- 실제 점주 계정의 매장 정보를 조회해 사이드바에 표시하도록 수정했다.
- TypeScript 타입 검사와 프로덕션 빌드를 통과했고, 개발 서버 응답과 `/api` 백엔드 프록시 연결을 확인했다.
- 초대용 이메일·임시 비밀번호로 가입하는 회원가입 페이지를 추가하고 `/api/auth/signup`에 연결했다.
- 비밀번호 재설정 요청과 새 비밀번호 설정 페이지를 추가하고 일회용 토큰 API에 연결했다.
- 로그인, 회원가입, 비밀번호 재설정 화면에 공통 인증 레이아웃과 비밀번호 입력 컴포넌트를 적용했다.
- 로그인된 사용자가 인증 전용 페이지에 다시 접근하지 못하도록 게스트 라우트를 추가했다.
- 관리자와 점주의 공통 `내 정보 및 보안` 페이지를 구현하고 실제 계정·프로토타입 세션을 구분해 표시했다.
- 실제 브라우저에서 인증 화면 레이아웃, 잘못된 재설정 링크 처리와 콘솔 오류 여부를 확인했다.

### 결정 사항

- 로컬 개발에서는 Vite의 `/api` 프록시를 사용하고, 배포 환경에서는 `VITE_API_BASE_URL`로 백엔드 주소를 주입한다.
- 로그인 상태 유지 선택 시 토큰을 로컬 저장소에, 선택하지 않으면 세션 저장소에 보관한다.
- 프로토타입 계정은 API를 호출하지 않고 기존 데모 데이터를 사용하며, 실제 계정만 백엔드 데이터를 사용한다.
- 온라인 커머스와 고객·쿠폰 도메인의 요청·응답 타입 및 API 호출은 각각 `entities/commerce`, `features/commerce`에서 관리한다.
- 회원가입 역할은 프론트에서 선택하지 않고 백엔드 초대 정보로만 결정한다.
- 계정 존재 여부 노출을 막기 위해 비밀번호 재설정 요청 결과는 이메일 등록 여부와 관계없이 동일하게 안내한다.

### 미해결 이슈

- 관리자 IAM과 대시보드·매출·AI·재고·리뷰 화면은 아직 목업 데이터를 사용한다.
- 백엔드에 토큰 재발급 API와 HttpOnly 쿠키 정책이 추가되면 현재 토큰 저장 방식을 재검토해야 한다.
- 온라인 주문 API가 구현되면 주문 조회·상태 변경 화면을 추가해야 한다.
- 실제 백엔드와 데이터베이스를 함께 실행하는 통합/E2E 테스트를 추가해야 한다.
- 운영 환경에서 비밀번호 재설정 이메일이 전달되도록 SMTP 환경변수를 구성해야 한다.
- 이름·연락처를 화면에서 수정하려면 백엔드 프로필 수정 API가 필요하다.

## 2026-07-27

### AWS SES 비밀번호 재설정 화면 연동

#### 작업 로그

- 회원가입 화면에서 로그인 전용 옵션인 `rememberMe` 전달을 제거했다.
- 비밀번호 찾기 화면에서 개발용 토큰 노출 UI를 제거하고 이메일 확인 안내 흐름으로 변경했다.
- 비밀번호 재설정 요청 API가 본문 없는 성공 응답을 처리하도록 프론트엔드 계약을 수정했다.
- 깨져 있던 비밀번호 찾기 화면의 한글 문구를 정상화했다.
- TypeScript 타입 검사와 프로덕션 빌드를 통과했다.

#### 결정 사항

- `rememberMe`는 로그인 화면에만 제공한다.
- 재설정 토큰은 브라우저 화면에 직접 노출하지 않고 이메일 링크의 쿼리 파라미터로만 전달한다.
- 계정 존재 여부와 관계없이 사용자에게 동일한 이메일 확인 안내를 표시한다.

#### 미해결 이슈

- 실제 SES 발송 이메일에서 `/reset-password?token=...` 진입 및 비밀번호 변경까지 E2E 검증이 필요하다.
- 운영 프론트엔드 주소가 정해지면 `PASSWORD_RESET_FRONTEND_URL`을 해당 HTTPS 주소로 변경해야 한다.

### 작업 로그

- 모든 API 요청에 Refresh Cookie를 전달하도록 credentials 정책을 추가했다.
- Access Token 만료 시 `/api/auth/refresh`를 한 번만 호출하고 실패한 원래 요청을 자동 재시도하도록 구현했다.
- 여러 API가 동시에 만료되어도 하나의 재발급 요청만 공유하도록 동시성 처리를 추가했다.
- 앱 시작 시 Access Token이 없어도 Refresh Cookie로 인증 세션을 복원하도록 변경했다.
- 실제 계정 로그아웃 시 서버 Refresh Token을 폐기하고, 역할 불일치 로그인과 데모 계정 전환 시 남은 쿠키를 정리하도록 연결했다.
- 비밀번호 재설정 화면을 AWS SES 도입 전 개발용 토큰 흐름에 맞게 간소화했다.
- TypeScript 타입 검사와 프로덕션 빌드가 통과했다.

### 결정 사항

- Refresh Token은 JavaScript에서 읽거나 저장하지 않고 백엔드가 설정한 HttpOnly Cookie만 사용한다.
- Access Token은 기존 저장 정책을 유지하되 만료 시 자동으로 교체한다.
- Refresh 요청 실패 시 로컬 Access Token과 사용자 상태를 정리하고 로그인 화면으로 전환한다.

### 미해결 이슈

- AWS SES 연동 후 비밀번호 재설정 화면을 이메일 발송 안내와 링크 진입 흐름으로 변경해야 한다.
- 실제 브라우저와 운영 도메인 환경에서 Secure·SameSite·CORS 쿠키 정책을 E2E 테스트해야 한다.

### 초대 이메일 기반 회원가입 전환

#### 작업 로그

- 임시 비밀번호 입력 회원가입을 이메일의 일회용 초대 링크 기반 가입 화면으로 변경했다.
- 가입 화면 진입 시 초대 토큰을 검증하고 마스킹된 이메일, 역할, 만료 일시를 표시하도록 연동했다.
- 가입 요청에서는 초대 토큰과 사용자가 직접 설정한 비밀번호·프로필 정보만 전송하도록 변경했다.
- 최고관리자의 관리자 초대와 관리자의 점주 초대 화면을 실제 IAM 초대 API에 연결했다.
- 초대 관리 화면에서 임시 자격증명 복사 UI를 제거하고 이메일 발송 상태로 교체했다.
- 초대 토큰의 외부 유출을 줄이기 위해 전역 Referrer Policy를 `no-referrer`로 설정했다.
- TypeScript 타입 검사와 프로덕션 빌드가 통과했다.

#### 결정 사항

- 공개 회원가입은 제공하지 않고 `/signup/invitation?token=...` 링크로만 가입을 완료한다.
- 이메일과 역할은 수정 가능한 입력값으로 노출하지 않고 백엔드 초대 검증 결과만 표시한다.
- 관리자·점주 초대 시 현재 관리자 비밀번호를 다시 확인한다.

#### 미해결 이슈

- 초대 목록·재발송·취소 화면은 현재 프로토타입 데이터이므로 백엔드 관리 API 구현 후 실제 데이터로 교체해야 한다.
- SES Sandbox에서는 초대 수신 이메일도 검증되어 있어야 하므로 실제 팀원 대상 테스트 전 Sandbox 해제 여부를 확인해야 한다.
- 운영 프론트엔드 주소가 정해지면 `INVITATION_FRONTEND_URL`을 HTTPS 주소로 변경해야 한다.

## 2026-07-27 - 백엔드 main 인증·초대 연동

### 작업 로그

- 백엔드 `main`의 로그인, 현재 사용자 조회, 초대 기반 회원가입 API에 프론트엔드를 연결했다.
- SUPER_ADMIN의 관리자 초대와 ADMIN/SUPER_ADMIN의 점주 초대 API를 연결했다.
- 초대 생성 응답의 일회용 임시 비밀번호를 화면에 한 번 표시하고 복사할 수 있도록 구성했다.
- 로그인 상태 유지 여부에 따라 액세스 토큰을 세션 또는 로컬 저장소에 보관하도록 구성했다.
- IAM 화면은 URL 직접 접근을 포함해 SUPER_ADMIN만 접근할 수 있도록 보호했다.
- 기존 프로토타입 계정 전환 기능은 유지했다.
- TypeScript 타입 검사와 Vite 프로덕션 빌드를 통과했다.

### 결정 사항

- 백엔드 `main`에는 Refresh Token, 로그아웃, 비밀번호 재설정 API가 없으므로 프론트에서 호출하지 않는다.
- 회원가입은 공개 가입이 아니라 관리자가 발급한 이메일과 임시 비밀번호를 사용하는 초대 기반 가입으로 제공한다.
- 개발 환경에서는 Vite `/api` 프록시를 사용하고 기본 백엔드 주소는 `http://localhost:8081`로 둔다.

### 미해결 이슈

- 비밀번호 재설정은 백엔드 API가 병합된 이후 별도 연동이 필요하다.
- 초대 목록·재발급·취소 화면은 백엔드 관리 API와 실제 데이터 연동이 필요하다.
- 실제 백엔드와 데이터베이스를 함께 실행한 로그인·초대·회원가입 E2E 검증이 필요하다.

## 2026-07-27 - 관리자 점주 초대 화면

### 작업 로그

- 최고 관리자와 일반 관리자에게 공통으로 노출되는 `점주 초대` 네비게이션을 추가했다.
- `/admin/store-owner-invitations` 전용 화면과 점주 초대 절차 안내를 구현했다.
- 현재 관리자 비밀번호 확인 후 `/api/iam/invitation/store-owner`를 호출하도록 연동했다.
- 생성 결과의 일회용 임시 비밀번호를 한 번 표시하고 복사할 수 있도록 구현했다.
- 가맹점 관리의 기존 점주 초대 버튼은 중복 모달 대신 새 전용 화면으로 이동하도록 정리했다.
- TypeScript 타입 검사와 Vite 프로덕션 빌드를 통과했다.

### 결정 사항

- 점주 초대는 SUPER_ADMIN과 ADMIN이 모두 사용할 수 있으므로 최고 관리자 전용 IAM 메뉴와 분리해 공통 관리자 메뉴에 배치한다.
- 백엔드에 초대 목록 조회 API가 없으므로 프론트에 가짜 목록을 만들지 않고 실제로 동작하는 초대 생성 흐름만 제공한다.

### 미해결 이슈

- 점주 초대 목록·취소·재발급 기능은 관련 백엔드 API가 추가된 이후 구현해야 한다.

## 2026-07-27 - ACCOUNT 역할별 계정 관리

### 작업 로그

- 관리자 네비게이션에서 통합 대시보드 아래에 `ACCOUNT` 카테고리를 추가했다.
- 최고 관리자에게 관리자 계정·점주 계정·초대 관리·IAM 로그를 표시했다.
- 일반 관리자에게 점주 계정·초대 관리만 표시했다.
- 관리자 계정과 점주 계정 목록 및 활성화·비활성화를 실제 백엔드 API에 연결했다.
- 관리자 계정 페이지 하단에는 관리자 초대 현황, 점주 계정 페이지 하단에는 점주 초대 현황을 배치했다.
- 통합 초대 관리 화면은 최고 관리자에게 관리자·점주 초대를, 일반 관리자에게 점주 초대만 표시하도록 연동했다.
- 초대 취소와 IAM 로그 화면을 실제 백엔드 데이터에 연결했다.

### 결정 사항

- 계정 페이지에서 계정과 해당 역할의 초대를 함께 확인할 수 있도록 구성한다.
- 별도 점주 초대 네비게이션은 점주 계정 페이지의 초대 기능으로 통합한다.
- 백엔드가 허용하는 데이터 범위를 그대로 사용하고 프론트 목업 계정·초대·IAM 로그는 제거한다.

### 미해결 이슈

- 계정·초대 데이터가 많아지면 서버 페이지네이션과 검색 기능이 필요하다.
- 초대 재발급은 백엔드 API 추가 후 제공한다.

## 2026-07-27 - 관리자 네비게이션 STORE 분류

### 작업 로그

- 관리자 사이드바에서 ACCOUNT 외 운영 메뉴를 `STORE` 카테고리로 분류했다.

### 결정 사항

- 통합 대시보드는 최상단에 독립적으로 유지하고, 계정·권한 기능은 ACCOUNT, 매장 운영 기능은 STORE로 구분한다.

### 미해결 이슈

- 없음.

## 2026-07-28 - 점주 매장·커머스·고객·쿠폰 화면 연동

### 작업 로그

- 점주용 `매장·커머스` 화면을 매장 정보, 상품 관리, 온라인 판매, 할인 관리 탭으로 재구성했다.
- 매장과 상품의 등록·수정, 상품 상태 변경, 온라인 판매 등록·해제, 온라인 매장 개점·폐점을 백엔드 API에 연결했다.
- 등록 상품을 대상으로 할인 생성과 상태 변경을 할 수 있도록 연동했다.
- 고객 목록·등록과 쿠폰 발급·조회·회수 화면을 백엔드 API에 연결했다.
- 매장 미등록, 검색 결과 없음, 처리 중, API 오류 등 주요 상태를 화면에 표시하도록 구성했다.
- 프로필은 이름, 역할, 계정 상태, 이메일, 연락처와 로그아웃만 남겨 간결하게 정리했다.
- TypeScript 타입 검사와 Vite 프로덕션 빌드를 통과했다.

### 결정 사항

- 온·오프라인 상품은 하나의 상품 원장을 사용하고, 온라인 판매 탭에서는 기존 상품의 온라인 등록 여부만 관리한다.
- 매장 온라인 판매는 판매 가능한 온라인 등록 상품이 있을 때만 열 수 있도록 UI에서 선행 조건을 안내한다.
- 고객 개인정보는 목록에 필요한 이름·연락처·이메일만 노출하고, 기술 식별자와 인증 정보는 프로필에서 표시하지 않는다.

### 미해결 이슈

- 고객 수정·삭제와 쿠폰 사용 처리는 현재 백엔드 API가 없어 조회·등록·발급·회수 범위만 제공한다.
- 실제 주문 API가 추가되면 온라인 판매 현황과 쿠폰 사용 상태를 주문 흐름에 연결해야 한다.

## 2026-07-28 - 프론트엔드 CI 구성

### 작업 로그

- GitHub Actions 기반 프론트엔드 CI 워크플로우를 추가했다.
- `main` 대상 PR과 `main` 반영 시 `npm ci`, TypeScript 타입 검사, 프로덕션 빌드를 실행하도록 구성했다.
- npm 의존성 캐시, 최소 권한, 중복 실행 취소, 실행 시간 제한과 수동 실행을 적용했다.

### 결정 사항

- 프로젝트의 Node 타입 정의와 호환되는 Node.js 22를 CI 기준 버전으로 사용한다.
- 테스트와 린트 스크립트가 아직 없으므로 현재 CI는 타입 검사와 빌드를 필수 검증으로 사용한다.
- 배포와 빌드 아티팩트 업로드는 CD 도입 시 별도 워크플로우로 구성한다.

### 미해결 이슈

- 단위 테스트와 린트 도구를 도입하면 CI 검증 단계에 추가해야 한다.
- `npm ci` 검사에서 확인된 보통 심각도 취약점 3건과 유지보수가 종료된 Recharts 2.x의 업그레이드를 별도 검토해야 한다.

## 2026-07-28 - 초대 비밀번호 오류의 잘못된 로그아웃 수정

### 작업 로그

- 공통 API 클라이언트가 모든 HTTP 401 응답을 세션 만료로 처리하던 문제를 수정했다.
- 백엔드 오류 코드가 토큰 만료·누락·손상 등 실제 인증 세션 오류인 경우에만 액세스 토큰을 제거하도록 변경했다.
- 관리자·점주 초대의 현재 비밀번호 불일치는 로그인 상태를 유지하면서 서버 메시지를 초대 화면에 표시하도록 구성했다.

### 결정 사항

- HTTP 상태나 한글 오류 메시지가 아닌 백엔드의 안정적인 오류 코드를 기준으로 세션 만료 여부를 판단한다.

### 미해결 이슈

- 없음.

## 2026-07-29 - AI 배너 이미지 밝기 재조정

### 작업 로그

- 어두운 차콜 계열의 카페 이미지를 밝은 회색, 내추럴 우드, 부드러운 자연광이 적용된 실제 카페 분위기의 이미지로 교체했다.
- 아이스 아메리카노와 무지 크라프트 컵홀더를 유지하고 불필요한 로고와 문구는 넣지 않았다.

### 결정 사항

- 배너와의 색상 연결은 어두운 배경 대신 블루그레이 가구와 금속 집기의 은은한 색감으로 표현한다.
- 상품의 가독성과 자연스러운 인상을 위해 밝은 확산광과 충분한 여백을 우선한다.

### 미해결 이슈

- 없음.

## 2026-07-29 - AI 배너 이미지 경계 개선

### 작업 로그

- PC 화면에서 남색 문구 영역과 카페 이미지가 수직선처럼 나뉘던 경계를 개선했다.
- 이미지 영역을 문구 영역 안쪽으로 겹치고 넓은 다단계 그라데이션을 적용해 두 영역이 자연스럽게 이어지도록 수정했다.

### 결정 사항

- 작은 화면에서는 기존 가로 그라데이션을 유지하고, PC 화면에서만 확장된 이미지와 넓은 페이드 효과를 적용한다.

### 미해결 이슈

- 없음.

## 2026-07-29 - AI 상품 이미지 배너 비주얼 강화

### 작업 로그

- 브랜드를 사용하지 않은 AI 생성형 아이스 아메리카노 판매 이미지를 신규 제작해 프로젝트 자산으로 추가했다.
- AI 상품 이미지 배너를 어두운 블루·바이올렛 계열의 전용 스튜디오 영역으로 재구성했다.
- 상품 사진 선택, AI 분위기 생성, 판매 이미지 완성의 3단계 흐름과 생성 결과 미리보기를 추가했다.
- CTA와 AI 생성 이미지의 대비를 강화해 커머스 화면에서 기능을 즉시 인지할 수 있도록 개선했다.

### 결정 사항

- 생성 이미지는 특정 브랜드나 사용자가 제공한 사진을 복제하지 않은 독립적인 프로젝트 자산으로 사용한다.
- 배너에서는 기술 설명보다 AI가 만들어 주는 판매 결과물을 먼저 보여준다.

### 미해결 이슈

- 없음.

## 2026-07-29 - 사업자등록번호 실시간 형식 검증

### 작업 로그

- 사업자등록번호 입력값을 `000-00-00000` 형식으로 자동 정리하도록 구현했다.
- 숫자 10자리가 완성되지 않으면 입력 필드 바로 아래에 한글 검증 메시지를 표시하도록 추가했다.
- 형식 오류가 있는 동안 매장 등록 버튼을 비활성화했다.
- 백엔드 오류 메시지에 영문 필드명이 포함되더라도 사용자에게는 한글 메시지만 표시되도록 보강했다.

### 결정 사항

- 빈 입력은 필수값 검증으로 처리하고, 사용자가 입력을 시작한 이후부터 형식 오류를 안내한다.
- 프론트엔드와 백엔드 모두 하이픈 유무와 관계없이 같은 사업자등록번호 규칙을 사용한다.

### 미해결 이슈

- 없음.

## 2026-07-28 - 관리자 네비게이션 정보 구조 개선

### 작업 로그

- 관리자와 최고 관리자 네비게이션을 업무 우선순위에 따라 재배치했다.
- 통합 대시보드를 독립 메뉴로 유지하고 ACCOUNT, STORE, ANALYTICS, BUSINESS, SYSTEM 카테고리로 분리했다.
- 최고 관리자와 일반 관리자의 계정 관리 권한에 따른 메뉴 노출 조건을 유지했다.
- 관리자 레이아웃에 모바일 사이드바와 메뉴 열기·닫기 동작을 추가했다.
- 축소 사이드바의 메뉴 툴팁과 주요 버튼의 접근성 이름을 추가했다.

### 결정 사항

- 계정과 초대 업무는 기존 요구사항에 따라 통합 대시보드 바로 아래 ACCOUNT에 배치한다.
- 가맹점 운영, 분석, 계약·공지, 시스템 상태를 서로 다른 업무 카테고리로 분리한다.
- 최고 관리자만 관리자 계정과 IAM 로그를 확인하며, 일반 관리자는 점주 계정과 초대 관리만 사용한다.

### 미해결 이슈

- 없음.

## 2026-07-28 - 점주 네비게이션 정보 구조 개선

### 작업 로그

- 점주 대시보드는 사이드바 최상단에 독립적으로 유지했다.
- `매장·커머스`를 대시보드 바로 아래 STORE 카테고리의 첫 메뉴로 이동했다.
- 나머지 점주 메뉴를 STORE, AI, ANALYTICS 카테고리로 구분하고 업무 우선순위에 맞게 재배치했다.
- 축소된 사이드바에서도 메뉴의 접근성 이름과 툴팁을 확인할 수 있도록 보완했다.

### 결정 사항

- STORE에는 매장·커머스, 재고·발주, 고객·쿠폰을 배치한다.
- AI에는 AI 전략 추천과 AI 가계부를 배치한다.
- ANALYTICS에는 매출 분석, 지출·원가, 리뷰 분석, 경영 리포트를 배치한다.

### 미해결 이슈

- 없음.

## 2026-07-28 - Refresh Token 기반 세션 갱신 연동

### 작업 로그

- 모든 API 요청에 HttpOnly Refresh Token 쿠키가 포함되도록 credentials 설정을 추가했다.
- Access Token 만료로 401 응답이 발생하면 Refresh API를 호출하고 원래 요청을 한 번 재시도하도록 구현했다.
- 백엔드 오류 코드를 확인해 현재 비밀번호 불일치와 같은 업무 오류에서는 세션을 갱신하거나 종료하지 않도록 구성했다.
- 동시에 여러 요청이 만료되어도 Refresh API는 한 번만 호출하도록 단일 갱신 Promise를 적용했다.
- 로그인 유지 선택값을 로그인 API에 전달하고 기존 Access Token 저장 위치를 유지하도록 구성했다.
- 로그아웃과 역할 불일치 시 백엔드 세션 폐기 API를 호출하도록 연동했다.

### 결정 사항

- 프론트엔드는 Refresh Token을 직접 읽거나 localStorage·sessionStorage에 저장하지 않는다.
- Refresh Token 재발급이 실패하거나 재사용이 탐지되면 Access Token과 사용자 세션을 제거한다.
- 로그인 유지 여부는 Access Token 저장 위치와 Refresh Token 쿠키의 지속 시간을 함께 결정한다.

### 미해결 이슈

- 여러 브라우저 탭에서 동시에 갱신할 가능성이 커지면 BroadcastChannel을 이용한 탭 간 갱신 조정을 추가 검토한다.

## 2026-07-28 - 온라인 커머스와 AI 상품 이미지 생성 통합

### 작업 로그

- `main` 병합 과정에서 발생한 라우터와 공통 API 클라이언트 충돌을 해결했다.
- 기존 매장·상품·온라인 판매·할인 관리 화면을 `/store/commerce`에 유지했다.
- AI 상품 이미지 생성 화면을 `/store/commerce/product-images` 하위 경로로 분리하고 양방향 이동 버튼을 추가했다.
- 원시 응답 처리 기능과 백엔드 오류 코드 기반 세션 처리 로직을 함께 유지하도록 API 클라이언트를 통합했다.
- 프로덕션 빌드와 브라우저 화면 이동을 검증했다.

### 결정 사항

- AI 상품 이미지 생성은 온라인 커머스를 대체하지 않고 커머스의 보조 기능으로 제공한다.
- 온라인 커머스의 기본 진입점은 통합 관리 화면으로 유지한다.

### 미해결 이슈

- 백엔드 상품 이미지 API가 실행되지 않은 환경에서는 카테고리 목록을 불러올 수 없다.
- 생성된 이미지를 특정 상품의 이미지로 즉시 저장하는 연동은 별도 기능으로 검토한다.

## 2026-07-29 - 매장 등록 모달 오류 표시 개선

### 작업 로그

- 매장 등록 및 수정 요청 오류를 페이지 공통 배너가 아닌 매장 모달 내부에 표시하도록 상태를 분리했다.
- 입력값을 수정하거나 모달을 닫고 다시 열면 이전 오류가 초기화되도록 처리했다.
- 백엔드 검증 메시지의 영문 필드명 접두사를 제거해 사용자 안내 문구만 노출했다.
- 오류 및 성공 안내 영역에 스크린 리더용 ARIA 역할을 추가했다.

### 결정 사항

- 모달에서 시작한 저장 요청의 오류는 해당 모달 안에서 처리한다.
- 목록 조회와 상태 변경처럼 페이지에서 시작한 요청의 오류는 기존 페이지 배너를 유지한다.

### 미해결 이슈

- 없음.

## 2026-07-29 - AI 상품 이미지 진입 영역 개선

### 작업 로그

- 페이지 헤더의 작은 AI 상품 이미지 버튼을 제거하고 커머스 본문 상단에 전용 기능 배너를 추가했다.
- AI 배경 합성 기능의 목적과 사용 효과를 설명하고 명확한 실행 버튼을 배치했다.
- 기존 파란색 중심 디자인에 보라색 보조 강조색을 사용해 다른 화면과의 통일성을 유지했다.
- 콘텐츠 탭의 활성 상태를 단색 파란색에서 옅은 강조 배경으로 변경해 화면의 시각적 무게를 정돈했다.

### 결정 사항

- AI 상품 이미지 생성은 상품 등록과 동일한 최상위 작업 버튼이 아니라 커머스의 주요 보조 기능으로 안내한다.
- AI 배너는 모든 커머스 탭에서 확인할 수 있도록 매장 요약 영역 위에 고정 배치한다.

### 미해결 이슈

- 없음.

## 2026-07-29 - 커머스 정보구조 및 온라인 판매 흐름 개선

### 작업 로그

- 커머스 화면에서 매장 정보 탭을 제거하고 상품 관리 화면을 기본 탭으로 변경했다.
- 점주 내 정보 화면에 매장 상세정보 조회 및 수정 영역을 추가했다.
- 상품 관리 화면의 `상품 원장` 표현을 `등록 상품`과 `상품 목록`으로 변경했다.
- 온라인 판매를 상품이나 재고 등록 전에 먼저 열 수 있도록 버튼 조건과 안내 문구를 수정했다.
- 페이지 우측 상단의 중복 상품 등록 버튼을 제거했다.
- 매장 요약, 탭, 상품·온라인 판매·할인 카드의 배경과 테두리 대비를 보강했다.
- AI 상품 이미지 배너 문구를 판매 효과 중심으로 개선했다.

### 결정 사항

- 커머스 화면에는 상품·온라인 판매·할인 업무만 배치하고 민감한 매장 상세정보는 내 정보에서 관리한다.
- 매장의 온라인 영업 상태와 온라인 판매 상품 등록 상태는 서로 독립적으로 관리한다.

### 미해결 이슈

- 없음.

## 2026-07-29 - AI 배너 상품 이미지 자연스러움 개선

### 작업 로그

- 배너 상품 이미지를 따뜻한 카페 인테리어와 자연스럽게 어우러지는 아이스 아메리카노 사진으로 다시 생성했다.
- 파란색 컵홀더와 BP20 텍스트를 제거하고 무지 크라프트 컵홀더로 변경했다.
- 커피의 조명 방향, 색온도, 접지 그림자와 피사계 심도를 배경에 맞춰 합성 느낌을 줄였다.

### 결정 사항

- 배너 예시 이미지는 브랜드 표현보다 실제 점주가 사용할 수 있는 자연스러운 판매 이미지 품질을 우선한다.

### 미해결 이슈

- 없음.

## 2026-07-29 - AI 배너 와이드 화면 초점 보정

### 작업 로그

- 1536px 이상 PC 화면에서 배너 이미지의 초점을 왼쪽으로 이동해 커피가 오른쪽 끝에 치우치지 않도록 조정했다.
- 1440×900 노트북과 1920×1080 PC 해상도에서 배너 너비, 가로 스크롤 및 이미지 위치를 각각 검증했다.

### 결정 사항

- 노트북과 PC의 이미지 패널 너비 차이를 고려해 `xl`과 `2xl` 구간에 서로 다른 이미지 초점값을 적용한다.

### 미해결 이슈

- 없음.

## 2026-07-29 - 점주 화면 병합 충돌 및 배너 자산 경로 정리

### 작업 로그

- 점주 레이아웃 병합 충돌을 해결하고 그룹형 네비게이션에 공지사항 메뉴를 통합했다.
- 접근성 속성과 인쇄 화면 처리를 모두 유지하도록 상단 헤더 충돌을 병합했다.
- AI 상품 이미지 배너를 `public/images`에서 `src/shared/assets/images`로 이동하고 모듈 import 방식으로 연결했다.

### 결정 사항

- 화면 코드가 직접 사용하는 이미지 자산은 Vite 빌드 시 누락 여부와 해시 경로를 검증할 수 있도록 `src/shared/assets`에서 관리한다.
- `public`은 파비콘, robots.txt 등 원본 파일명과 고정 URL을 유지해야 하는 공개 자산에 사용한다.

### 미해결 이슈

- 없음.
